import React from "react";
import Screen from "./Screen";

export const Form = ({ navigation }) => (
  <Screen navigation={navigation} name="Form" />
);

export const ProfileScreen = ({ navigation }) => (
  <Screen navigation={navigation} name="Profile" />
);
